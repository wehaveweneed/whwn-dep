<li class="geocode-result" data-latitude="<%= place.lat %>" data-longitude="<%= place.lon %>">
    <% console.log(place); %>
    <div class="result-name">
        <a class="geocode-result-link" href="#" data-latitude="<%= place.lat %>" data-longitude="<%= place.lon %>"><%= place.display_name %></a>
    </div>
</li>
